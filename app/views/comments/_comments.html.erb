<div class="p-3 mb-3 bg-light rounded" id="comments"">
    <% commentable.comments.each do |comment|%>

        <p class="mb-0">
            <%= comment.user.email %>
            <%= comment.text %></li>
        </p>
        <br>

    <% end %>

</div>